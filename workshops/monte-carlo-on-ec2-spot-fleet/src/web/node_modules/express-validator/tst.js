const { check } = require('./check');
const app = require('express')();

app.post('/*', check('foo').equals('bar'));

app.post('/foo', (req, res, next) => {
  res.json(req._validationErrors);
});

app.listen(3000);